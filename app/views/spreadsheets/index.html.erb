<div class='container'>
  <% @rows.each do |row| %>
    <div class='row'>
      <div class='top_line'>
        <span class='ru_number'><%= row.ru_number %></span>
        <span class='outside_inside'><%= row.outside_inside %></span>
      </div>
      <%= row.truncated_description %>
    </div>

  <% end %>
</div>
